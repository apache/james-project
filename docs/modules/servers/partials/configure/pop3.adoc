Consult this link:{sample-configuration-prefix-url}/pop3server.xml[example] for guidance on how to configure POP3.

The POP3 service is configured in the file `pop3server.xml`.
It contains one `pop3servers` element that can contain multiple `pop3server` elements.

The `pop3server` element can be configured using the common options described xref:{pages-path}/configure/server.adoc[here].

Additionally, it contains a mandatory `handlerchain` element that configures the behavior of the server using handlers.
The `handlerchain` element has the following optional attributes:

- `coreHandlersPackage` is a string specifying a handler loader. All handlers of this loader are added to the end of the handlerchain. The default is `org.apache.james.pop3server.core.CoreCmdHandlerLoader`.
- `enableJmx` enables the JMX for this server. It is a boolean which is `true` by default.
- `jmxHandlersPackage` is a string specifying a handler loader for JMX. All handlers of this loader are added to the handlerchain after the core handlers. The default is `org.apache.james.pop3server.jmx.JMXHandlersLoader`. It only has an effect if `enableJmx` is `true`.

The `handlerchain` element may contain an arbitrary number of `handler` elements.
Every `handler` element must have the string attribute `class`.
They are inserted in order at the beginning of the handlerchain.
The handlers of the `coreHandlersPackage` or `jmxHandlersPackage` do not need their own `handler` elements.
